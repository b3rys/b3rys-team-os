# 기여하기 (Contributing)

b3os에 관심 가져주셔서 고맙습니다. 🙏

> 🌐 한국어·English 둘 다 환영합니다. 이슈·PR을 편한 언어로 써주세요.

---

## 먼저 알아두실 것

b3os는 **gd.on의 개인 프로젝트로 시작한 오픈소스**입니다. 그래서 솔직하게 미리 말씀드립니다:

- **macOS(애플 실리콘)에서만 개발·테스트됐습니다.** 윈도우·리눅스는 지금 삐걱댑니다.
- 유지보수 인원이 적어 **리뷰가 늦을 수 있습니다.** 재촉하셔도 괜찮습니다.
- 큰 변경은 **먼저 이슈로 이야기해 주세요.** 다 만들고 나서 방향이 안 맞으면 서로 아깝습니다.

작은 것부터 환영합니다 — 오타, 안 맞는 문서, 헷갈리는 에러 메시지, 재현되는 버그 리포트.

---

## 개발 환경

```bash
git clone https://github.com/b3rys/b3rys-team-os.git
cd b3rys-team-os

bun install --frozen-lockfile   # lockfile 그대로 (CI와 같은 의존성)
bun run dev                     # 개발 서버 — http://127.0.0.1:7878/team
```

| | 버전 | 비고 |
|---|---|---|
| **bun** | `1.3.14` | CI가 이 버전으로 고정돼 있습니다. 다르면 우리 변경과 무관하게 깨질 수 있습니다. |
| **macOS** | 애플 실리콘 | `tmux`가 필요합니다(설치 스크립트가 깔아줍니다). |

> ⚠️ 개발 서버도 `127.0.0.1` 전용이고 **앱 레벨 인증이 없습니다.** 공용 네트워크에 열지 마세요.

---

## 보내기 전에 확인할 것

```bash
bunx tsc --noEmit    # 타입체크 — CI가 도는 것과 같습니다
bun test             # 테스트
```

**`bun test` 기대값:**

| 환경 | 결과 |
|---|---|
| macOS | **1756 pass · 5 skip · 0 fail** (1761개, 152파일) — 여기서 실패하면 여러분의 변경이 원인일 가능성이 큽니다 |
| Linux (CI 러너 등) | **7건 알려진 실패** — zsh 부재·LaunchAgent·로컬 LLM 의존. **여러분 탓이 아닙니다** |

숫자가 위와 다르면 PR 본문에 그대로 적어주세요. **초록으로 맞추려고 테스트를 고치지는 말아주세요.**

---

## PR 절차

1. **브랜치를 파세요.** `main`에 직접 푸시하지 않습니다. 이름은 `fix/…`, `feat/…`, `docs/…` 처럼요.
2. **PR 템플릿을 채워주세요.** 특히 이 두 칸:
   - **검증 범위** — 실제로 돌린 명령. 범위를 안 밝히면 *좁혀서 만든 초록*과 구분되지 않습니다.
   - **미검증** — 확인하지 **않은** 범위. 빈칸은 "없음"이 아니라 "안 적음"으로 읽힙니다.
3. **CI**는 지금 **타입체크만** 돕니다. **머지를 막지 않습니다** — 검사가 상시 빨간불이면 아무도 안 보게 되고, 그러면 진짜 실패가 묻히기 때문입니다. 테스트는 초록 이력이 쌓인 뒤 넣습니다.
4. **리뷰**에서 봐줬으면 하는 곳을 적어주시면 훨씬 빨라집니다. 스스로 의심스러운 지점이면 충분합니다.

### 공개 저장소라서 지키는 것

- **`main`에 force push 하지 않습니다.** 이미 나간 커밋을 되돌릴 때는 `git revert`를 씁니다 — 남의 클론을 깨뜨리지 않는 유일한 방법입니다.
- **태그는 옮기지 않습니다.** 잘못 붙였으면 새 버전을 냅니다.
- 자기 브랜치를 정리할 때만 force를 쓰고, 그때도 `--force-with-lease`를 씁니다.

---

## 커밋 메시지

특별한 규칙은 없지만, **무엇을 왜 바꿨는지**가 보이면 좋습니다.

```
fix(bus): self-route 가드가 direct_to_gd 를 통과시키던 것
docs: 준비물 표에 Claude 구독 명시
```

---

## 버그 리포트

[이슈 템플릿](.github/ISSUE_TEMPLATE/bug_report.md)을 써주세요. 가장 도움이 되는 건 **재현 절차**와 **기대한 것 vs 실제로 본 것**입니다.

> 🔐 로그·스크린샷을 붙이실 때 **봇 토큰·API 키·`.env` 내용이 섞이지 않았는지** 한 번만 봐주세요. 공개 저장소라 한 번 올라가면 지워도 남습니다.

---

## 보안 취약점

공개 이슈로 올리지 마시고 [`SECURITY.md`](.github/SECURITY.md)의 절차를 따라주세요.

---

## 라이선스

기여하신 코드는 **[Apache License 2.0](LICENSE)** 으로 배포됩니다. PR을 여는 것으로 그에 동의하신 것으로 봅니다.

> "b3os"·"b3rys" 이름과 로고는 b3rys의 상표입니다. 코드는 자유롭게 쓰시되, 이름·로고를 제품명이나 브랜딩에 쓰시려면 별도로 문의해 주세요.
